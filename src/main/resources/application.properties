# Application Name
spring.application.name=eventapp

# ---------- DATABASE CONFIGURATION ----------

# ? Use environment variables or cloud DB URI instead of localhost (localhost won't work in cloud)

# MySQL (For cloud deployment, change 'localhost' to your cloud DB URI)
spring.datasource.url=${MYSQL_DB_URL:jdbc:mysql://localhost:3306/event_db}
spring.datasource.username=${MYSQL_DB_USERNAME:root}
spring.datasource.password=${MYSQL_DB_PASSWORD:12345siva@2005A}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# MongoDB (localhost won't work ? use cloud URI like MongoDB Atlas if deployed)
spring.data.mongodb.uri=${MONGO_DB_URL:mongodb://localhost:27017/eventdb}

# ---------- SERVER CONFIGURATION ----------
# ? Use PORT from environment on Render or fallback to 8080 locally
server.port=${PORT:8080}

# ---------- MAIL CONFIGURATION ----------
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=eventmanager2736@gmail.com
spring.mail.password=cnrx ltdb tytn lona
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.protocol=smtp
spring.mail.properties.mail.debug=true
